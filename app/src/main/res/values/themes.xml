<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Базовая тема приложения -->
    <style name="Theme.EduAppML" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Без тулбара и тайтла -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>

        <!-- Рисуем сами под барами -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- Полная прозрачность системных баров и фона окна -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>

        <!-- Работа с «чёлкой» и отключение автоконтраста на старых девайсах -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>

    <!-- Системный Splash (Android 12+). ВАЖНО: наследуется от библиотечной
         Theme.SplashScreen, а НЕ от Theme.EduAppML выше — поэтому
         windowActionBar/windowNoTitle оттуда сюда не наследуются
         автоматически и должны быть продублированы явно здесь. Раз
         MainActivity — единственная Activity на всё приложение (остальные
         экраны — это Compose-навигация внутри неё), фича ActionBar
         фиксируется окном именно в момент создания под этой темой, до
         срабатывания кода в onCreate() — отключать её через код в
         requestWindowFeature() уже поздно, окно её проигнорирует. Только
         отключение здесь, в самой теме, до создания окна, реально работает. -->
    <style name="Theme.EduAppML.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="postSplashScreenTheme">@style/Theme.EduAppML</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
